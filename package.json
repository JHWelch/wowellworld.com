{
  "name": "wowellworld.com",
  "version": "1.0.0",
  "description": "",
  "main": "public/index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i ./src/app.css -o ./public/app.css --watch",
    "build": "npx tailwindcss -i ./src/app.css -o ./public/app.css --minify",
    "prod": "npm run build"
  },
  "author": "Jordan Welch",
  "license": "MIT",
  "devDependencies": {
    "tailwindcss": "^3.2.7"
  }
}
